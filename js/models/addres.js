// Aqui, vai ser um módulo para especificar o tipo do meu endereço.

export default function Addres(cep,  street, number, city)/*entre os *()* recebo os meus argumentos*/{
    this.cep = cep;
    this.street = street;
    this.number = number;
    this.city = city;
}